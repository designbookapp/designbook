{
  "name": "context-arch-evals",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Baseline eval harness for the context-architecture experiment. NOT published.",
  "scripts": {
    "baseline": "node src/run.ts",
    "curated": "node src/run.ts --arch curated",
    "dry-run": "node src/run.ts --mock",
    "dry-run-curated": "node src/run.ts --mock --arch curated",
    "dashboard": "node src/dashboard.ts",
    "viewer": "node src/serveViewer.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.6",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2"
  }
}
