/**
 * Flow types — authored design intent for which screens exist, their order,
 * and rendering. Flow content comes from the user's designbook config; see
 * `flows.ts` for the synthesized fallback.
 */

export type {
  Flow,
  FlowPreview,
  FlowScreen,
  WireframeKind,
} from "@designbookapp/designbook/config";
