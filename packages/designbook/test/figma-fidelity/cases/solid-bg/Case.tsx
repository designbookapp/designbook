/** solid-bg — fixed-size div, background-color + opacity. Tier H P. */
export function SolidBg() {
  return (
    <div
      style={{
        width: 200,
        height: 120,
        backgroundColor: "rgb(37, 99, 235)",
        opacity: 0.9,
      }}
    />
  );
}
